# Brand accent + font only. Base theme is left to follow the viewer's system,
# so the in-app System/Light/Dark toggle and custom CSS can layer on top.
[theme]
primaryColor = "#2F54EB"
font = "sans serif"
